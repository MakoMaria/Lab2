#include <iostream>
#include <set>

using namespace std;


int main() {
    int n;
    cin >> n;
    set<int> arr;
    for (int i = 0; i < n; i++) {
        int k;
        cin >> k;
        arr.insert(k);
    }

    cout << arr.size();
    cout << endl;

    return 0;
}
